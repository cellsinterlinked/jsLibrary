let days =['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday'];

console.log(days.toString())
//This is a method to turn arrays into one string

console.log(days.length);
//will give the string and 5 for the number of days

let soup = {
    a: 'chicken noodle',
    b: 'tomato',
    c: 'beef and barley',
    };
    /*
    objects are ket value pairs, which just means in an object there are keys(what the
    name of this part of the object is),
    and a value for that key.
    
    */
   console.log(soup.a)
   // will run and give you answer chicken noodle
   //this is called dot notation for an object (referencing data within an object)
   //this is how we can dig through an object to get the data back we want

   let car = "Honda";

   let dec = 10.2;

